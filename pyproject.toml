[project]
name = "stash"
version = "0.1.0"
description = "A microblog for photos, reading notes, and short memos"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
