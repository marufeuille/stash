node_modules/
dist/
.astro/
.vault-tmp/

# content/ is sourced from stash-vault repo at build time via scripts/sync-content.mjs
# For local dev: set STASH_VAULT_PATH=/path/to/local/vault, then `npm run sync-content`
content/
